/**
 * Constants related to mongodb user configuration.
 */
export enum MongoUserConfigConstants {
    /**
     * Name of the user collection in mongodb.
     */
    USER_COLLECTION_NAME = 'users',
    /**
     * Name of the document provider to interact with the user collection.
     */
    USER_DOCUMENT = 'USER_DOCUMENT',
}
